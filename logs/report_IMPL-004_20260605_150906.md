# 報告 IMPL-004 — 生成時 OBS 自動サマリの焼き込み(点検くろうど段階1 第一弾)

- 日時: 2026-06-05 15:09
- 担当: 実装くろうど
- トークン: 0(AIエージェント不使用・コードに観測ログを足しただけ)

## 1. 何をやったか(一言)
画像を生成するたびに「観測サマリ」が自動で1ブロック出るようにした。
これまで手打ちしていた `show_obs()` が不要になり、UI から生成したときも
「顔データ(id_embeds)が入ったか/入らなかったか・なぜ入らなかったか」が必ず残る。

## 2. 結果(何を出すようになったか)
生成の最後に、ログへ次の1ブロックが自動で出る(`[OBS-SUMMARY]`):
- pipe_base_class : どのパイプラインで動いたか
- hyper_flux      : enable / loaded / 実 steps_pass1(実際に回ったステップ数)
- id_embeds       : 有✅ / 無⚠️(顔データが入ったか)
- ↳ 失敗理由      : 「無」のときだけ、なぜ無かを併記
  (PuLID 抽出エラー extract と 初期化エラー init の両方)
- faces_detected  : n/a(single-ref) ← 後述の理由で「正直に無意味と表示」

あわせて指示3点(RECON-005 由来)も実施:
- FIX-1: 「無」のとき理由を出す → 上の「↳ 失敗理由」で対応。
- FIX-2: faces_detected は参照画像1枚運用だと意味を持たない数字なので、
  判断材料に使わせないため「n/a(single-ref)」と正直表示に変更。
- FIX-3: 顔抽出が空(None)なのに「✅ 抽出完了 shape=?」と誤って成功ログを
  出していた箇所を修正。これからは「⚠️ get_id_embedding が None(顔未検出の可能性)」
  と正しく警告し、その理由がサマリにも伝わる。

### 検証(完了条件)
- py_compile: 2ファイルとも OK(構文エラーなし)。
- 変更範囲: `03_identity_engine.py` と `05_orchestrator.py` の2ファイルのみ
  (差分 +45 / -5 行、追加はすべて観測ログ)。
- 不変の確認: Setting A の7値・PuLID 集約核・量子化核は一切触っていない。
  silent fail(try/except: pass)は新設していない。サマリ自体は getattr で
  安全に値を読むため、サマリの不具合で生成が止まることはない。

## 3. 残課題
- 実際の生成を1回回して `[OBS-SUMMARY]` が期待どおり出るか(特に「無⚠️ + 失敗理由」)
  の実機確認はまだ。これは Colab 反映後でないとできない(下記参照)。
- FIX-3 は指示の主対象(1st attempt)に加え、リサイズ再試行(retry)経路にも
  同じ誤成功ログがあったため、同じ直しを当てた(同一不具合・1行修正・整合性のため)。
  指示の明示範囲を1か所超えた判断なので、念のため報告に明記。

## 4. 要・司令部判断の事項
### ★【保留】Drive 同期(G:\マイドライブ)が未完 — Colab 反映の前提が欠けている
- 4拠点のうち、拠点2 `C:\Users\yuuki\Downloads\AIBOV7` と
  拠点3 `...\3a` へは反映済み・MD5 一致(2ファイルとも MATCH)を確認。
- しかし **G ドライブが現在マウントされていない**(`G:` 自体が見えない)ため、
  Colab が読む `G:\マイドライブ\aibo_v7\` への同期ができていない。
- これは CLAUDE.md が警告する事故経路そのもの(「直したのに Colab は旧ファイル」)。
  → **G ドライブをマウントしてから、Drive 同期 + MD5 確認をやり直す必要あり。**
  再実行用スクリプト: `.errorfix\_impl004_sync.ps1`(G マウント後にこれを流せば
  3拠点コピー + MD5 照合まで自動。現状は拠点2/3 MATCH・G は DRIVE-NOT-MOUNTED 表示)。

### push は未実施(指示どおり)
- 指示「GitHub push は不要(notebook 不変)/ push は司令部確認後」に従い、
  commit / push はしていない。ローカル作業ツリーのみ変更。

## 5. 次の一手の提案(決定は司令部)
1. みやちんが G ドライブをマウント → `.errorfix\_impl004_sync.ps1` を1回実行し、
   G の2ファイルが MD5 MATCH になるのを確認。
2. Colab 側で `os.kill(os.getpid(), 9)` でランタイム再起動 → Cell 0 から再 run。
   ※ コードを直しただけでは Colab には反映されない(再 run 必須)。
3. 生成を1回実行し、ログ末尾の `[OBS-SUMMARY]` が出ること、特に id_embeds が
   「無⚠️」のとき失敗理由が併記されることを目視確認(これで段階1第一弾の実機完了)。

## 完了状態(phase-gate)
- コード実装・構文確認・拠点2/3 同期: 完了。
- Drive(G)同期: **完了(2026-06-05 追記。下記 ★追記 参照)。**
- Colab 反映・実機での出力確認: **未完(Colab 側で再 run が必要)。**
  → コード反映・4拠点同期までは完了。Colab 実機確認は残課題。

---

## ★追記(2026-06-05 15:1x) — Drive(G)同期 完了
- 経緯: 当初 G ドライブが未マウント(Google Drive for Desktop 自体が未起動)だった。
  GoogleDriveFS を起動 → G: マウント → フォルダ構造のストリーミング完了後に同期実行。
- 結果: **4拠点すべて MD5 一致を確認(G 含む)。**
  - 03_identity_engine.py : MD5 `889941BC29E585ABE791CB00A3C6FB81` — origin/拠点2/拠点3/G すべて MATCH
  - 05_orchestrator.py    : MD5 `1C301A8698598262BC964F85DA5C38F2` — origin/拠点2/拠点3/G すべて MATCH
  - スクリプト出力: `RESULT: ALL-4-LOCATIONS-MATCH`
- 補足(ハマりどころ・次回用): Windows PowerShell 5.1 は BOM 無し .ps1 を ANSI(cp932)で読むため、
  スクリプト中に日本語リテラル「マイドライブ」を直書きすると実フォルダ名とバイト不一致になり
  Test-Path が誤って ABSENT を返す。回避策として、G:\ 直下から「aibo_v7 を含むフォルダ」を
  ファイルシステム実体から動的検出して宛先を組み立てる ASCII 安全版
  `.errorfix\_impl004_sync_v2.ps1` を使用(以後の Drive 同期はこちで)。

## 残課題(更新)
- Colab 反映: `os.kill(os.getpid(), 9)` でランタイム再起動 → Cell 0 から再 run。
  ※ コード反映済みでも Colab は再 run しないと旧コードのまま。
- 実機で生成を1回回し、ログ末尾の `[OBS-SUMMARY]` 出力(特に id_embeds「無⚠️」時の失敗理由併記)を目視確認。
- push は未実施(指示どおり・司令部確認後)。
