# 報告 [IMPL-001] — safetensors ロード前検証ゲート(C0)実装完了

- 日付: 2026-05-31 17:28
- 担当: 統括(遂行管理)+ 実装くろうど(実装)
- 指示: IMPL-001（司令部決定: RECON-002 段階導入の第1段=C0 のみ / fail-fast + 詳細ログ)
- 根拠: docs/design/recon_002_drivefuse_design_20260531.md（C0）

## 1. 何をやったか(一言)
モデルファイル(safetensors)を**読み込む前に「途中で壊れていないか」を点検**する仕組みを
04_pipeline_manager.py に追加した。壊れていたら**黙って素の Flux（＝外国人化）に落ちず、
はっきりエラーを出して起動を止める**ようにした。点検は header だけ読む軽量方式で、生成速度に影響しない。

## 2. 結果(完了条件をすべて満たした)
実装くろうどが実装、統括が完了条件を検証（phase-gate）した。

- **点検関数 `verify_safetensors` 単体テスト: 合格**
  - 正常ファイル → `(True, 'ok')`
  - 末尾を1バイト削った壊れファイル → `(False, 'not-fully-covered expected=73 actual=72')`
- **構文確認: `python -m py_compile 04_pipeline_manager.py` → OK**
- **点検フックの配線（コードレビューで確認・file:行）**:
  - (a) 本体モデル(Nunchaku transformer) — `04_pipeline_manager.py:362`（最重要・外国人化の主疑い）
  - (b) ベースモデル(FLUX.1-dev: VAE/テキストエンコーダ) — `:425`
  - (c) ControlNet(Shakker Union Pro 2.0) — `:562`
  - (d) Hyper-FLUX LoRA — `:1096`
  - 点検失敗時: `logger.error("[C0] FAIL <名前> safetensors: <理由> expected=.. actual=.. path=..")` を出して
    専用例外 `C0VerificationError` を raise。`build()` がこれを捕捉し `❌ 構築失敗` + traceback を出して
    **False を返す（＝パイプライン未構築で停止。素 Flux 縮退には落ちない）**。
  - 点検成功時: `logger.info("[C0] OK <名前> <path>")` を1行。
- **既存ロジック破壊なし**: 追加は「点検フック＋壊れていたら止める」のみ（91行・全て挿入、削除0行）。
  既存のロード・分岐は不変。fail-fast が既存の degrade-except に飲まれないよう、
  (b) は縮退 except の外側に配置、(d) Hyper-FLUX は `except C0VerificationError: raise` を先頭に足して
  C0 エラーだけは降格させない配線にした。
- **厳守ルール無抵触**: Setting A の7値 / PuLID 集約 / 量子化核 / 指定外の from_pretrained に未接触。
  `try/except: pass`（silent fail）の新設なし。

## 3. 4拠点同期(★完了・MD5 一致)
04_pipeline_manager.py を4拠点にコピーし MD5 全一致を確認:
- MD5 = `bc1abffe3321dfb234a75831e30d8127`（主 / Downloads/AIBOV7 / 同 3a / G:\マイドライブ\aibo_v7 すべて一致）
- ※ G ドライブ(Colab 参照)も一致確認済み。

## 4. 残課題 / 注意
- **実機未起動**: 本タスクは「配線まで」。実際に Colab で起動して [C0] ログが出るかは PO 実施。
- **点検の解決範囲**: 単一ファイル系(本体モデル/Hyper-FLUX)は HF cache の実体パスを直接解決して点検。
  リポ全体系(ベース/ControlNet)は cache 済み *.safetensors を列挙して best-effort 点検。
  解決できない場合は握りつぶさず `[C0] SKIP ... ` を **warn** で残す（見逃しの可視化）。
- **commit 同梱物の開示**: 04_pipeline_manager.py には本セッション前から作業ツリーにあった
  OBS-001 由来の観測ログ3行（`[OBS-A]` 2行＋status の `pipe_base_class`）が含まれる。
  これは実装くろうどの追加ではなく既存の未コミット変更で、統括が委譲前の Read で存在を確認済み。
  C0 commit に同梱される形になる（1ファイルのため分離不可）。

## 5. 要・司令部判断 / 次の一手
- **push は保留（司令部確認後）**。現状はローカル commit のみ（master）。
  04_pipeline_manager.py は Colab パイプラインコードのため、push 時は colab-stable も対象になりうる
  （PC ツールではない）。両 push 可否を司令部判断願う。
- **Colab 再 run が必要**: PC 側修正は Colab 実行に自動反映されない。
  PO が Colab で `os.kill(os.getpid(), 9)` → Cell 0 から再 run。
- **起動後の観測（司令部報告事項）**:
  - (a) `[C0] FAIL ...` が出て起動が止まった → **外国人化 = Drive 縮退が確定**（C0 が真因捕捉）。次は案A へ。
  - (b) `[C0] OK ...` が全て通り正常起動 → 今回は転送成功。生成して OBS-A/B/C を確認へ。
- 第2段（案A: HF 直 DL 一次化）/ 案C は **C0 の結果＝真因確定を見てから**司令部が指示（今回スコープ外）。
