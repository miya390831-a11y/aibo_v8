# AIBO Cyber Studio v7.5.0 · Day 5-6 引継書 v4

**作成日**: 2026-05-27 深夜
**前回**: HANDOVER_2026-05-24_v3.md
**作業期間**: Day 5 (2026-05-27) 約 8 時間
**達成**: Claude Code CLI 完全移行 + 13 タスク完了 + 初の生成成功 + Phase A 実装

---

## Part 1: Day 5 達成サマリ

### 完了タスク (13 個)

```
 #  タスク                                  commit      所要
 1  ファイル配置 + Claude Code 起動          (セットアップ)  -
 2  #001.5 デスクトップアイコン              ea19cbd     15 min
 3  #001.6 bypass モード設定                 9b05ae6     10 min
 4  #001.7 ログ運用ルール                    8aa2ff2     10 min
 5  #001 Setting A 焼き付け                  10d349c     15 min
 6  #001.8 rsync → tar pipe 修正             7f968b7     20 min
 7  #001.9 numpy 再 install ループ撲滅       7f926a7     15 min
 8  #001.10 エラー自動診断・自己修復         5e30634     30 min
 9  diagnostic: Pass1 silent fail 解消        5a13e83     15 min
10  diagnostic: Pass1 リトライ強化            2b84499     15 min
11  diagnostic: PuLID bind_forward 修正       adb5a40     15 min
12  AIBO 起動成功 + 初生成                   (実機確認)   -
13  #002 Phase A 参照集約改善                 2ca916b     15 min
```

### 推論性能

```
生成成功: 1024×1536 · 28-29 秒
推論改善: 32.8s → 28.9s (-12%) ← Setting A 効果
```

### Git 履歴 (Day 5 全コミット)

```
2ca916b feat(identity): multi-ref quality-weighted mean 集約 Phase A (#002)
adb5a40 fix(pulid): bind_forward 毎回再バインド + 防御ガード (#diagnostic)
2b84499 fix(orchestrator): Pass1 リトライ強化 + silent fail 根絶
5a13e83 fix(orchestrator): Pass1 空画像の silent fail 解消 + seed リトライ
5e30634 feat(diagnostics): 生成エラー自動診断・自己修復ループ構築 (#001.10)
7f926a7 fix(deps): numpy 再起動ループを根本修正
7f968b7 fix(sync): rsync を tar pipe + timeout に置換 · Drive FUSE ハング対策
10d349c feat(config): Setting A を新 default に確定
287c026 docs(logs): report_001_7
8aa2ff2 feat(logs): 完了報告ログ運用ルール導入 (#001.7)
9b05ae6 feat(security): bypass mode + 多層防御 + 緊急停止 (#001.6)
ea19cbd feat(desktop): Claude Code + Colab デスクトップアイコン整備 (#001.5)
```

---

## ★ Day 5 重要発見 3 点 (必読)

### ★ 1. Phase0Sniper の存在 (今夜判明)

```
場所:   05_orchestrator.py 72-183 行 (クラス定義)
呼出:   05_orchestrator.py 474-498 行 (generate() 内)
機能:   YOLOv8-face → 40% マージンタイトクロップ
デフォルト: ON · 全 reference 画像に常時自動適用
目的:   EVA-CLIP が背景・髪・帽子を ID と誤認 → ベレー帽幻覚防止
モデル: arnabdhar/YOLOv8-Face-Detection (HuggingFace)

処理順: ユーザー Upload → Frontend CropDialog(手動) → API → Phase0Sniper(自動) → PuLID
        ↑ 2 層のクロップが重なっている
```

### ★ 2. Phase A 検証手順 (明朝用)

```
Step 1: 通常フロー (Sniper ON + 2 ref) で再生成  ← まずこれ
        同じ reference + 同じプロンプトで:
        (a) 1 枚 reference → 旧動作と同じか (回帰テスト)
        (b) 2 枚 reference → Phase A 集約効果の確認

Step 2: 結果評価
        → 改善あり → Phase B 着手
        → 改善なし → Step 3 へ

Step 3: 必要なら Sniper バイパス比較 (基本不要)
        Phase0Sniper を一時的に無効化して比較
        → quality_score の挙動差を分析
        → 差があれば Phase B で調整
```

### ★ 3. 「外国人化」問題の真因 (確定)

```
真因: PuLID の西洋顔バイアス (訓練データ偏り)
      ↑ クロップ問題でも集約問題でもない

根拠:
  - 単一 reference + 単一 seed でも「外国人化」発生
  - Setting A のパラメータ調整では解消せず
  - 2 reference でも改善せず (burstiness が加わるのみ)

結論:
  Phase A/B (集約改善) は burstiness 緩和に寄与するが
  「外国人化」の根本解決には A1/A3 (Week 1+) が必要
  → A1/A3 の重要性が Day 5 の実機検証で確定した
```

---

## Part 2: 判明した課題 — 顔似度問題

### 症状

```
「外国人化」: 日本人 reference → 西洋人風の顔が出力される
「綺麗な別人」: 本人に似ていないが美しい別人が生成される
```

### 検証結果

```
✗ Setting A の顔似度向上効果: 出ず (推論時間効果のみ動作確認)
✗ 2 reference でも別人化 (burstiness 問題)
✗ 単一 seed × 単一 reference でも「外国人化」
```

### 原因推測

```
1. PuLID の西洋顔バイアス (訓練データ偏り)
2. IP-Adapter + CLIP-L の ethnicity leak
3. FLUX 本体の guidance 引力 (generic beauty)
4. 単純 mean embedding → burstiness で「平均顔」化
```

### 解決方針

```
短期 (Day 6):
  - #002 Phase B: cluster medoid 集約 (pose ベース)
  - トリミング有無での比較検証

中期 (Week 1-4):
  - A1: Identity-aware Quantization Residual Injection
  - A3: Closed-loop Identity Drift Correction
  - time-dependent ID gain (#003)

長期:
  - InfiniteYou / FLUX.2 への移行検討
  - 独自 identity encoder 訓練
```

---

## Part 3: ★ Reference 画像トリミング機能 (重要)

### 2 層構成が判明

AIBO の reference 画像処理は **2 層のトリミング** を経由する:

#### 層 1: Frontend (ユーザー手動クロップ)

```
ファイル: frontend/components/aibo/crop-dialog.tsx
ライブラリ: react-easy-crop
動作: ユーザーがアップロード時に顔エリアを手動選択
出力: 1024×1024 にリサイズされた base64 画像
呼び出し: portrait-mode.tsx → handleFile → setCropTarget → CropDialog
```

#### 層 2: Backend (YOLO 自動クロップ)

```
クラス: Phase0Sniper (05_orchestrator.py:72-183)
モデル: YOLOv8-face (arnabdhar/YOLOv8-Face-Detection)
動作: 生成直前に自動で顔検出 → 40% マージン正方形クロップ
目的: EVA-CLIP が背景/髪/帽子を ID と誤認 → ベレー帽幻覚を防止
呼び出し: orchestrator.generate() → self.sniper.snipe(ref_img)
```

#### 処理フロー

```
ユーザーが画像アップロード
  ↓
層 1: CropDialog (手動選択 · 1024×1024)
  ↓
base64 で API に送信
  ↓
層 2: Phase0Sniper (YOLO 自動 · 40% margin)
  ↓
Phase A: quality-weighted mean 集約 (multi-ref の場合)
  ↓
PuLIDExtractor → embedding 抽出
```

### Day 6 検証への影響

```
Phase A の quality_score は「Phase0Sniper 適用後」の画像で計算される。
つまり:
  - size_score は Phase0Sniper のマージン設定に依存
  - sharpness_score もクロップ後の画像に依存
  - 「全身 vs バストアップ」の差は Phase0Sniper が吸収

検証パターン:
  A) 通常フロー (層 1 + 層 2 両方): デフォルト
  B) Phase0Sniper バイパス: 層 1 のみ
  C) 両方バイパス: 生の全身画像

→ まず A (通常フロー) で 2 枚 reference テストを推奨
→ 差があれば B, C を追加検証
```

---

## Part 4: #002 Phase A 実装の詳細

### 変更ファイル

| ファイル | commit | md5 |
|---------|--------|-----|
| 01_config.py | 2ca916b | 9b7ae234b01409732be9a19073c8d33f |
| 03_identity_engine.py | 2ca916b | 02631b869b1973088322eaf31ca0b754 |

### 変更内容

```python
# 01_config.py
IdentityConfig.get_reference_images()  # 新旧 API 統一

# 03_identity_engine.py
PuLIDExtractor.extract(face_image)     # Image | list[Image] を両対応
PuLIDExtractor._extract_single()       # 旧 extract() をリネーム
PuLIDExtractor._compute_quality_score()# InsightFace ベース品質スコア
PuLIDExtractor._extract_quality_weighted() # softmax 重み付き平均 + ノルム復元
IdentityEngine.prepare()               # multi-ref → PuLID 集約対応
```

### Quality Score の計算式

```
score = 0.40 * size_score      # 顔面積 / 画像面積 * 5.0 (cap 1.0)
      + 0.35 * sharpness_score # Laplacian 分散 / 200.0 (cap 1.0)
      + 0.25 * det_score       # InsightFace 検出信頼度
```

### 集約戦略

```
Phase A (現在): quality_weighted_mean
  weight_i = softmax(quality_i / temperature=0.5)
  final_emb = Σ(weight_i * emb_i) → ノルム復元

Phase B (次): cluster medoid (未実装)
  pose でクラスタ化 → 各クラスタの quality 最大 medoid → softmax 集約
```

---

## Part 5: Day 6 タスク (優先順)

### P0: #002 検証 (トリミング有無 2 パターン)

```
1. Colab 再起動 + Cell 0 完走確認
2. 同じ reference + 同じプロンプトで再生成:
   - 1 枚参照 → 旧動作と同じか確認 (回帰テスト)
   - 2 枚参照 → 「外国人化」「綺麗な別人」が改善されたか
3. トリミング検証:
   - A) 通常フロー (デフォルト) でまず試す
   - B) 差があれば Phase0Sniper バイパスで比較
```

### P1: #002 Phase B (cluster medoid 集約)

```
_cluster_by_pose() → pose の近い参照をクラスタ化
_select_quality_medoid() → 各クラスタの quality 最大を選出
4 段階: scoring → cluster → medoid → softmax 集約
```

### P2: #005 Pose Templates (足切れ対策)

```
10 種類のポーズテンプレート
ControlNet Pose で骨格強制注入 → 足切れ回避
```

### P3 (任意): #003 time-dependent ID gain

```
PuLID weight を timestep で山型変動
[0-15%] base * 0.5 → [30-70%] base * 1.2 → [85-100%] base * 0.8
```

### P4 (任意): #004 guidance scheduler

```
guidance_scale を単調増加スケジュール化
初期の強すぎる guidance → generic beauty 引力を抑制
```

---

## Part 6: Week 1+ ロードマップ

### A1: Identity-aware Quantization Residual Injection

```
概要: Nunchaku INT4 の low-rank branch に identity 専用 rank を連結
効果: weight 空間からの identity 注入 (PuLID の CA 注入と直交)
実装量: ~400 行 (identity_residual_injector.py)
特許: 出願 #2 候補
```

### A3: Closed-loop Identity Drift Correction

```
概要: 中間 latent → identity 計測 → drift 検知 → weight 補正
Tier-1: PuLID hidden cosine (~5ms)
Tier-2: TAEF1 decode + ArcFace (~30-80ms)
実装量: ~500 行 (a3_drift_correction.py)
特許: 出願 #1 候補 (PuLID 著者が放棄したパスを迂回)
```

### 特許 3 件構成

```
出願 #1: A3 単独 (Closed-loop identity-preserving sampling)
出願 #2: A1 単独 (Quantization-residual identity correction)
出願 #3: 統合 (Feedforward + feedback dual-loop) ← 最強 moat
```

---

## Part 7: 技術的注意事項 (厳守)

### 絶対禁止

```
❌ Setting A の 7 値を変更しない
   GFPGAN=0.0, pulid_weight=0.70, sigma_start=0.25, sigma_end=0.90,
   ip_adapter_weight=0.75, cn_depth_guidance_end=0.65,
   pass2_strength=0.34, pass2_pulid_boost=1.25

❌ PHASE3B_ENABLED = True にしない
❌ ACE++ 関連コード復活させない
❌ pulid_double_interval を inference 時変更しない
❌ 負の PuLID weight を設定しない
❌ IP-Adapter 依存を深めない (2026/03 廃止予定)
❌ silent fail 禁止 (try/except:pass 禁止)
❌ qweight を直接編集しない (A1 ルール)
```

### 必須手順

```
✅ コード変更時は 4 拠点同期:
   1: C:\Users\yuuki\aibo_v7\              (主)
   2: C:\Users\yuuki\Downloads\AIBOV7\     (BK1)
   3: C:\Users\yuuki\Downloads\AIBOV7\3a\  (BK2)
   4: G:\マイドライブ\aibo_v7\              (Drive · Colab 参照)
   → MD5 4 拠点一致を検証

✅ Git push は master + colab-stable 両方

✅ 完了報告は logs/report_XXX.md に標準テンプレートで保存
```

---

## Part 8: ファイル状態 (Day 5 完了時点)

### Day 5 で変更済みファイル

```
01_config.py              Setting A + get_reference_images()
02_colab_setup.py         rsync → tar pipe + numpy 修正
03_identity_engine.py     Phase A multi-ref + quality-weighted
05_orchestrator.py        Pass1 リトライ + silent fail 修正
04_pipeline_manager.py    PuLID bind_forward 修正
error_reporter.py         新規 · 自動診断システム
start_claude_code.bat     新規 · bypass 起動
start_aibo.bat            新規 · フロントエンド起動
emergency_stop.bat        新規 · 緊急停止
setup_desktop_shortcuts_full.bat  新規 · アイコン整備
sync_4_locations_v2_bridge.ps1    新規 · 同期スクリプト
CLAUDE.md                 Claude Code 運用ルール
logs/README.md            ログ運用テンプレート
```

### Day 6 で変更予定

```
03_identity_engine.py     Phase B cluster medoid 追加
05_orchestrator.py        Phase B 統合 (変更量 少)
01_config.py              Pose template config 追加 (#005)
```

### 完了報告ファイル一覧

```
logs/report_001_7_20260527_181000.md        #001.7 ログ運用ルール
logs/report_001_20260527_182028.md          #001 Setting A 焼き付け
logs/report_001_8_20260527_184218.md        #001.8 rsync → tar pipe
logs/report_001_9_20260527_185531.md        #001.9 numpy ループ撲滅
logs/report_001_10_20260527_192203.md       #001.10 自動診断
logs/report_diagnostic_001_20260527_194845.md  Pass1 silent fail
logs/report_diagnostic_002_20260527_200416.md  PuLID bind_forward
logs/report_002_phase_a_20260527_202957.md  #002 Phase A
```

---

## Part 9: 明朝の手順

```
1. Claude Code 起動
   start_claude_code.bat (デスクトップ 🥷)

2. 状態確認
   "git log --oneline -5"
   → 最新が 2ca916b (Phase A) であること確認

3. Colab 再起動
   Cell 0 完走 → AIBO 起動成功確認

4. 検証
   同じ reference + 同じプロンプトで:
   (a) 1 枚参照で旧動作確認 (回帰テスト)
   (b) 2 枚参照で Phase A 効果確認

5. 結果に応じて:
   → 改善あり: Phase B 着手
   → 改善なし: トリミング変数の分析
   → 顔似度問題が深刻: A1/A3 を Day 6 から前倒し
```

---

## Part 10: PO へ

```
Day 5 お疲れ様でした。

13 タスク完了 · AIBO 初生成成功 · Phase A 実装完了。
Claude Code CLI への完全移行が Day 5 で完了しました。

顔似度問題 (「外国人化」「綺麗な別人」) は残っていますが、
これは PuLID/IP-Adapter の根本的なバイアス問題であり、
A1/A3 で解決する設計になっています。

明朝は Phase A の検証から始めましょう。
2 枚 reference で改善が見られれば Phase B へ、
見られなければトリミング変数の分析に進みます。

ぐっすり休んでください。
```

---

AIBO Cyber Studio v7.5.0 · Day 5-6 引継書 v4
Generated by Claude Code (Opus 4.7) · 2026-05-27
